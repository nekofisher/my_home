<?xml version="1.0"?>

<!DOCTYPE MODE SYSTEM "xmode.dtd">

<!-- Trying to make a mode for nmc assembly language -->

<MODE>
    <PROPS>
    </PROPS>

    <RULES
        IGNORE_CASE="FALSE"
        HIGHLIGHT_DIGITS="FALSE" DIGIT_RE="[0-9][0-9a-zA-Z]*">
        <EOL_SPAN TYPE="COMMENT1">;</EOL_SPAN>
    </RULES>

</MODE>

